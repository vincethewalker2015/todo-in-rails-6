<% if @todo.errors.any? %>
  <h3>The Following Errors Prevented the Todo from Being Saved</h3>
    <ul>
      <% @todo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
<% end %>